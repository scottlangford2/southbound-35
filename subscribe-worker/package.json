{
  "name": "southbound-35-subscribe",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Southbound 35 newsletter subscription",
  "main": "src/worker.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "list-subs": "curl -s -H \"X-Admin-Token: $ADMIN_TOKEN\" \"$WORKER_URL/subscribers\" | jq"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.90.0"
  }
}
